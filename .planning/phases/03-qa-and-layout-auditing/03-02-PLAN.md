---
phase: 03
plan: 02
name: Simulation and Report Flow Verification
description: Inspect Home.vue, Process.vue, Step2EnvSetup.vue, Step3Simulation.vue, Step4Report.vue, Step5Interaction.vue, and HistoryDatabase.vue for layout regressions and functional integrity.
wave: 1
depends_on: []
autonomous: true
requirements_addressed: [QA-02]
files_modified:
  - frontend/src/views/Home.vue
  - frontend/src/views/Process.vue
  - frontend/src/components/Step2EnvSetup.vue
  - frontend/src/components/Step3Simulation.vue
  - frontend/src/components/Step4Report.vue
  - frontend/src/components/Step5Interaction.vue
  - frontend/src/components/HistoryDatabase.vue
---

# Plan 03-02 — Simulation and Report Flow Verification

## Scope
Validate that the simulation setup/execution and report viewing workflows are visually intact and functionally unbroken after the design system changes. Check key views for layout regressions and confirm functional entry points (buttons, inputs, routing) still work.

## Must Haves (derived from phase goal)
1. Home.vue console and engine-start button render correctly and route to Process.vue when clicked.
2. Process.vue step pipeline and navigation buttons retain their styling and routing behavior.
3. Step3Simulation.vue, Step4Report.vue, and Step5Interaction.vue layouts do not overflow or hide critical controls.
4. HistoryDatabase.vue cards conform to the new monochrome card contract.

---

<task>
  <id>1</id>
  <name>Verify Home.vue layout and engine-start flow</name>
  <read_first>
    - frontend/src/views/Home.vue
    - .planning/phases/02-component-refactoring/02-UI-SPEC.md
  </read_first>
  <action>
    1. Open frontend/src/views/Home.vue.
    2. Grep for `.start-engine-btn` and confirm:
       - `background: #000000;`
       - `color: #e2e2e2;`
       - `border-radius: 0px;`
       - Hover: `background: #f9f9f9; color: #000000; box-shadow: 4px 4px 0 #000000; transform: translate(-2px, -2px);`
       - Disabled: `opacity: 0.4; cursor: not-allowed; box-shadow: none;`
    3. Grep for `.code-input` and confirm its wrapper `.input-wrapper` has `background: #f3f3f3; border-radius: 0px;` and `.code-input` itself has `background: transparent; border-radius: 0px;`.
    4. Confirm the template still calls `startSimulation` on `.start-engine-btn` click and that `startSimulation` imports `setPendingUpload` and pushes `router.push({ name: 'Process', params: { projectId: 'new' } })`.
    5. Check that `.upload-zone` has a visible border (`border: 1px dashed #CCC;`) and reasonable min-height so the drop area is not collapsed.
    6. Fix any minor CSS regressions inline (e.g. padding, border-radius, hover shadow).
  </action>
  <acceptance_criteria>
    - grep -n "background: #000000" frontend/src/views/Home.vue returns a match in .start-engine-btn
    - grep -n "border-radius: 0px" frontend/src/views/Home.vue returns at least 3 matches
    - grep -n "box-shadow: 4px 4px 0 #000000" frontend/src/views/Home.vue returns a match
    - grep -n "background: #f3f3f3" frontend/src/views/Home.vue returns a match
    - grep -n "router.push({" frontend/src/views/Home.vue returns a match
    - grep -n "name: 'Process'" frontend/src/views/Home.vue returns a match
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <name>Verify Process.vue and Step2EnvSetup.vue pipeline styling</name>
  <read_first>
    - frontend/src/views/Process.vue
    - frontend/src/components/Step2EnvSetup.vue
    - .planning/phases/02-component-refactoring/02-UI-SPEC.md
  </read_first>
  <action>
    1. Open frontend/src/views/Process.vue.
    2. Grep for `.action-btn` inside the style block and confirm:
       - `border: 2px solid #000000;` or equivalent monochrome outline
       - `background: #f9f9f9;` or transparent
       - `border-radius: 0px;`
       - Hover includes `box-shadow: 4px 4px 0 #000000;`
    3. Grep for `.next-step-btn` and confirm:
       - `background: #000000;`
       - `color: #e2e2e2;`
       - `border-radius: 0px;`
    4. Confirm `goToNextStep` is present (even if it alerts "Not Implemented"; we only verify it exists and is wired to the button).
    5. Open frontend/src/components/Step2EnvSetup.vue.
    6. Confirm `.step-card` has `background: #e2e2e2;`, `border-radius: 0px;`, and no colored drop shadows.
    7. Confirm `.action-btn` inside Step2EnvSetup.vue matches the same monochrome button contract.
  </action>
  <acceptance_criteria>
    - grep -n "border-radius: 0px" frontend/src/views/Process.vue returns at least 2 matches
    - grep -n "box-shadow: 4px 4px 0 #000000" frontend/src/views/Process.vue returns a match
    - grep -n "background: #000000" frontend/src/views/Process.vue returns a match
    - grep -n "goToNextStep" frontend/src/views/Process.vue returns a match
    - grep -n "background: #e2e2e2" frontend/src/components/Step2EnvSetup.vue returns a match
    - grep -n "border-radius: 0px" frontend/src/components/Step2EnvSetup.vue returns at least 1 match
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <name>Verify Step3Simulation.vue, Step4Report.vue, and Step5Interaction.vue controls</name>
  <read_first>
    - frontend/src/components/Step3Simulation.vue
    - frontend/src/components/Step4Report.vue
    - frontend/src/components/Step5Interaction.vue
    - .planning/phases/02-component-refactoring/02-UI-SPEC.md
  </read_first>
  <action>
    1. Open frontend/src/components/Step3Simulation.vue.
    2. Confirm `.action-btn.primary` has `background: #000000;`, `color: #e2e2e2;`, `border-radius: 0px;`, and hover `box-shadow: 4px 4px 0 #000000;`.
    3. Confirm the timeline `.timeline-card` does not use any unexpected `border-radius` larger than 0px on outer containers that would break the brutalist aesthetic.
    4. Open frontend/src/components/Step4Report.vue.
    5. Confirm `.next-step-btn` has `border-radius: 0px;` and monochrome hover state.
    6. Open frontend/src/components/Step5Interaction.vue.
    7. Confirm `.chat-input` and `.survey-input` have `border-radius: 0px;` and a visible border or bottom border.
    8. Confirm `.send-btn` and `.survey-submit-btn` have `border-radius: 0px;` and monochrome styling.
    9. Verify that `handleNextStep` in Step3Simulation.vue still calls `generateReport` and routes to `{ name: 'Report' }`; verify Step4Report.vue's `goToInteraction` routes to `{ name: 'Interaction' }`.
    10. Fix any minor CSS regressions inline.
  </action>
  <acceptance_criteria>
    - grep -n "background: #000000" frontend/src/components/Step3Simulation.vue returns a match in .action-btn.primary
    - grep -n "border-radius: 0px" frontend/src/components/Step3Simulation.vue returns at least 1 match
    - grep -n "border-radius: 0px" frontend/src/components/Step4Report.vue returns at least 1 match
    - grep -n "border-radius: 0px" frontend/src/components/Step5Interaction.vue returns at least 2 matches
    - grep -n "name: 'Report'" frontend/src/components/Step3Simulation.vue returns a match
    - grep -n "name: 'Interaction'" frontend/src/components/Step4Report.vue returns a match
  </acceptance_criteria>
</task>

<task>
  <id>4</id>
  <name>Verify HistoryDatabase.vue monochrome card contract</name>
  <read_first>
    - frontend/src/components/HistoryDatabase.vue
    - .planning/phases/02-component-refactoring/02-UI-SPEC.md
    - .planning/phases/02-component-refactoring/02-CONTEXT.md
  </read_first>
  <action>
    1. Open frontend/src/components/HistoryDatabase.vue.
    2. Grep for `.project-card` and confirm:
       - `background: #e2e2e2;`
       - `border-radius: 0px;`
       - Hover: `box-shadow: 4px 4px 0 #000000; transform: translate(-2px, -2px);`
    3. Confirm `.card-bottom-line` has `height: 2px;` and `background: #000000;` (per D-03 in 02-CONTEXT).
    4. Confirm status icons use only monochrome tones: grep for `.status-icon.available` should show `color: #000000;` and `.status-icon.unavailable` should show a grey tone (`#D1D5DB` or similar grey).
    5. Confirm progress dots use only `#000000`, `#5e5e5e`, or `#c6c6c6` (per D-03). Specifically check `.card-progress.completed`, `.in-progress`, and `.not-started` classes.
    6. Fix any minor CSS regressions inline (e.g. restoring missing hover shadow, correcting a color token).
  </action>
  <acceptance_criteria>
    - grep -n "background: #e2e2e2" frontend/src/components/HistoryDatabase.vue returns a match
    - grep -n "border-radius: 0px" frontend/src/components/HistoryDatabase.vue returns at least 1 match
    - grep -n "box-shadow: 4px 4px 0 #000000" frontend/src/components/HistoryDatabase.vue returns a match
    - grep -n "background: #000000" frontend/src/components/HistoryDatabase.vue returns a match in .card-bottom-line context
    - grep -n "color: #000000" frontend/src/components/HistoryDatabase.vue returns a match in .status-icon.available
    - grep -n "color: #5e5e5e" frontend/src/components/HistoryDatabase.vue returns a match
    - grep -n "color: #c6c6c6" frontend/src/components/HistoryDatabase.vue returns a match
  </acceptance_criteria>
</task>

<verification>
  - [ ] Home.vue engine button and console inputs follow the monochrome contract
  - [ ] Process.vue and Step2EnvSetup.vue buttons and cards use 0px radius and block-shadow hover
  - [ ] Step3Simulation.vue, Step4Report.vue, and Step5Interaction.vue retain visible controls and monochrome styling
  - [ ] HistoryDatabase.vue cards use #e2e2e2 background, 0px radius, 2px black bottom line, and monochrome status dots
  - [ ] Simulation-to-report and report-to-interaction routing logic is preserved in all components
  - [ ] Any minor CSS regressions were fixed inline; no component state logic was modified
</verification>
