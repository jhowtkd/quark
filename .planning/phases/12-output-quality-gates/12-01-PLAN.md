# Phase 12: Output Quality Gates — Execution Plan

## Task 1: Extend `language_integrity.py` with language-switching detection
- Add `PORTUGUESE_STOPWORDS` and `ENGLISH_STOPWORDS` frozensets
- Add `detect_language_switches(text, expected_lang)` function
- Returns list of `(line_number, detected_lang, snippet)` tuples
- Update `__all__` if present

## Task 2: Create `backend/app/services/quality_gates.py`
Implement `QualityGateService` with:

### Inner Classes / Methods
- `LanguageConsistencyGate` — uses stopword density to detect language switches
- `KnownLimitationsGate` — checks for "Limitacoes Conhecidas" / "Known Limitations" heading
- `NumericConsistencyGate` — extracts numeric claims, detects contradictions
- `SelfContradictionGate` — detects logical opposite keywords in same report context

### Dataclasses
- `GateResult` — per-gate score, findings, metadata
- `QualityReport` — composite score, all gate results, warnings, is_acceptable

### Service API
- `QualityGateService.run_all(report_text, profile_lang="pt") -> QualityReport`

## Task 3: Update `profile_manager.py`
- Add to `ECONOMIA_CONFIG.report_system_prompt`: instruction to include "## Limitacoes Conhecidas" section
- Add `require_quality_gates: True` to `ECONOMIA_CONFIG`

## Task 4: Hook quality gates in `report_agent.py`
- Import `QualityGateService`, `QualityReport`
- In `generate_report()`, after bias audit block, add quality gates block
- Log quality gate results via `report_logger`
- Add quality gate metadata to `extra_meta` in `save_report()`
- In `assemble_full_report()`, call `_post_process_report()` then apply `KnownLimitationsGate.auto_append()` if section missing

## Task 5: Update `ProfileConfiguration` and `apply_to_report_agent()`
- Add `require_quality_gates: bool = False`
- Apply to report agent

## Task 6: Verify compilation
- Run `python -m py_compile` on all modified/new Python files
- Fix any syntax errors immediately

## Task 7: Update state files
- Create `12-01-SUMMARY.md`
- Update `ROADMAP.md` — mark Phase 12 Complete
- Update `REQUIREMENTS.md` — mark QUAL-01..04 Complete
- Update `STATE.md` — milestone v1.3 100% complete
