---
phase: 01-global-styling-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["frontend/index.html", "frontend/src/App.vue"]
autonomous: true
requirements: ["UI-01", "UI-02"]

must_haves:
  truths:
    - "Space Grotesk and Work Sans apply as the default fonts globally"
    - "Monochrome color variables are defined in the CSS framework"
  artifacts:
    - path: "frontend/index.html"
      provides: "Google Fonts import"
      contains: "family=Work+Sans:wght@300..700"
    - path: "frontend/src/App.vue"
      provides: "Global CSS variables"
      contains: "--color-primary:"
  key_links:
    - from: "frontend/src/App.vue"
      to: "body"
      via: "font-family: var(--font-human);"
      pattern: "font-family:\\s*var\\(--font-human\\)"
---

<objective>
Establish global typography and color variables according to the Blueprint Noir design system.

Purpose: Set the high-contrast monochrome baseline to replace the current theme.
Output: index.html with Google Fonts, App.vue with CSS custom properties.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-global-styling-foundations/01-CONTEXT.md
@.planning/research/DESIGN-SYSTEM.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Work Sans to Google Fonts</name>
  <files>frontend/index.html</files>
  <read_first>frontend/index.html</read_first>
  <action>
    Modify the existing Google Fonts link tag in `frontend/index.html`.
    Find the link: `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@100..800&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">`.
    Replace it with the exact string below to include Work Sans:
    `<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@100..800&family=Space+Grotesk:wght@300..700&family=Work+Sans:wght@300..700&display=swap" rel="stylesheet">`
  </action>
  <verify>
    <automated>grep -q "family=Work+Sans:wght@300..700" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains the exact string: `&family=Work+Sans:wght@300..700`
  </acceptance_criteria>
  <done>Work Sans is loaded via Google Fonts</done>
</task>

<task type="auto">
  <name>Task 2: Define Global CSS Tokens in App.vue</name>
  <files>frontend/src/App.vue</files>
  <read_first>frontend/src/App.vue, .planning/research/DESIGN-SYSTEM.json</read_first>
  <action>
    In `frontend/src/App.vue`, update the `<style>` block to define global tokens.
    1. Define `:root` with the following variables:
       `--color-primary: #000000;`
       `--color-background: #f9f9f9;`
       `--color-surface-container-low: #f3f3f3;`
       `--color-on-background: #1b1b1b;`
       `--color-on-primary: #e2e2e2;`
       `--color-surface: #f9f9f9;`
       `--color-surface-container-highest: #e2e2e2;`
       `--color-outline: #777777;`
       `--color-error: #ba1a1a;`
       `--font-machine: 'Space Grotesk', monospace;`
       `--font-human: 'Work Sans', sans-serif;`
    2. Update the `#app` rule to use the variables:
       ```css
       #app {
         font-family: var(--font-human);
         -webkit-font-smoothing: antialiased;
         -moz-osx-font-smoothing: grayscale;
         color: var(--color-on-background);
         background-color: var(--color-background);
       }
       ```
    3. Add rules for headers and buttons:
       ```css
       h1, h2, h3, .headline, .label, .display {
         font-family: var(--font-machine);
       }
       button {
         font-family: var(--font-machine);
       }
       ```
    Remove the previous `#app` rule that had hardcoded `#000000`, `#ffffff`, and `JetBrains Mono`.
  </action>
  <verify>
    <automated>grep -q "\-\-color-primary" frontend/src/App.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.vue` contains `--color-primary: #000000;`
    - `frontend/src/App.vue` contains `--font-machine: 'Space Grotesk'`
    - `frontend/src/App.vue` contains `font-family: var(--font-human);` inside `#app`
  </acceptance_criteria>
  <done>Blueprint Noir CSS tokens are defined globally</done>
</task>

</tasks>

<verification>
Check `npm run build` output or ensure the build works locally.
</verification>

<success_criteria>
- The font family `Work Sans` is successfully loaded and used in `#app`.
- Monochrome color variables are correctly declared in `:root`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-global-styling-foundations/01-01-SUMMARY.md`
</output>
