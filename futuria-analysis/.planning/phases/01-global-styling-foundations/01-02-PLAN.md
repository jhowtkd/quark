---
phase: 01-global-styling-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["frontend/src/views/Home.vue", "frontend/src/views/Process.vue", "frontend/src/components/LanguageSwitcher.vue", "frontend/src/views/LoginPage.vue", "frontend/src/views/RegisterPage.vue"]
autonomous: true
requirements: ["UI-02"]

must_haves:
  truths:
    - "Monochrome color variables replace existing primary colors in the Home page and main views"
    - "Monochrome color variables replace existing primary colors in Authentication pages"
  artifacts:
    - path: "frontend/src/views/Home.vue"
      provides: "Main view styling"
      contains: "var(--color-primary)"
    - path: "frontend/src/views/LoginPage.vue"
      provides: "Login view styling"
      contains: "var(--color-primary)"
  key_links:
    - from: "frontend/src/views/Home.vue"
      to: "App.vue variables"
      via: "var(--color-primary)"
      pattern: "var\\(--color-primary\\)"
---

<objective>
Replace the existing primary colors (like hardcoded --orange tokens) with the new global CSS tokens in all main views to establish the monochrome layout.

Purpose: Eliminate legacy color values and enforce the monochrome scheme.
Output: Refactored views and components utilizing `var(--color-primary)` instead of hardcoded hex values.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-global-styling-foundations/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace --orange in Main Views</name>
  <files>frontend/src/views/Home.vue, frontend/src/views/Process.vue, frontend/src/components/LanguageSwitcher.vue</files>
  <read_first>frontend/src/views/Home.vue, frontend/src/views/Process.vue, frontend/src/components/LanguageSwitcher.vue</read_first>
  <action>
    Remove any declarations of `--orange: #FF4500;` or `--orange: #FF6B35;` from the `<style scoped>` blocks.
    Replace all usages of `var(--orange)` and `var(--orange, #FF4500)` with `var(--color-primary)`.
    Specific changes:
    - In `Home.vue`, find `--orange: #FF4500;` and delete it. Replace `color: var(--orange);`, `background: var(--orange);`, `border-color: var(--orange);`, and `border-left: 3px solid var(--orange);` with `var(--color-primary)`.
    - In `Process.vue`, find `--orange: #FF6B35;` and delete it.
    - In `LanguageSwitcher.vue`, find `color: var(--orange, #FF4500);` and replace it with `color: var(--color-primary);`.
  </action>
  <verify>
    <automated>grep -q "\-\-orange" frontend/src/views/Home.vue frontend/src/views/Process.vue frontend/src/components/LanguageSwitcher.vue || true; ! grep -q "\-\-orange" frontend/src/views/Home.vue frontend/src/views/Process.vue frontend/src/components/LanguageSwitcher.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\-\-orange" frontend/src/views/Home.vue` returns no matches
    - `grep "\-\-orange" frontend/src/views/Process.vue` returns no matches
    - `grep "var(--color-primary)" frontend/src/views/Home.vue` returns at least one match
    - `grep "var(--color-primary)" frontend/src/components/LanguageSwitcher.vue` returns at least one match
  </acceptance_criteria>
  <done>Main views now strictly use global monochrome tokens</done>
</task>

<task type="auto">
  <name>Task 2: Replace --orange in Auth Views</name>
  <files>frontend/src/views/LoginPage.vue, frontend/src/views/RegisterPage.vue</files>
  <read_first>frontend/src/views/LoginPage.vue, frontend/src/views/RegisterPage.vue</read_first>
  <action>
    Remove declarations of `--orange: #FF4500;` from the `<style scoped>` blocks in auth views.
    Replace all usages of `var(--orange)` with `var(--color-primary)`.
    Specific changes:
    - In `LoginPage.vue`, find `--orange: #FF4500;` and delete it. Replace `color: var(--orange);` and `background: var(--orange);` with `var(--color-primary)`.
    - In `RegisterPage.vue`, find `--orange: #FF4500;` and delete it. Replace `color: var(--orange);` and `background: var(--orange);` with `var(--color-primary)`.
  </action>
  <verify>
    <automated>grep -q "\-\-orange" frontend/src/views/LoginPage.vue frontend/src/views/RegisterPage.vue || true; ! grep -q "\-\-orange" frontend/src/views/LoginPage.vue frontend/src/views/RegisterPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\-\-orange" frontend/src/views/LoginPage.vue` returns no matches
    - `grep "\-\-orange" frontend/src/views/RegisterPage.vue` returns no matches
    - `grep "var(--color-primary)" frontend/src/views/LoginPage.vue` returns at least one match
    - `grep "var(--color-primary)" frontend/src/views/RegisterPage.vue` returns at least one match
  </acceptance_criteria>
  <done>Auth views now strictly use global monochrome tokens</done>
</task>

</tasks>

<verification>
Check `npm run build` output or ensure the build works locally.
</verification>

<success_criteria>
- No legacy orange colors remain in the main and auth views.
- Components utilize `var(--color-primary)` for their main colored elements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-global-styling-foundations/01-02-SUMMARY.md`
</output>
