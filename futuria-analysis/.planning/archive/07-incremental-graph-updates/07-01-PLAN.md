---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/project.py
  - backend/app/services/text_processor.py
  - backend/app/services/graph_builder.py
  - backend/app/api/graph.py
  - backend/tests/services/test_graph_incremental.py
autonomous: true
requirements:
  - DELTA-01
  - DELTA-02
  - SAFE-02
must_haves:
  truths:
    - "Changed source content can be diffed against prior ingestion state."
    - "Only changed chunks are sent through the incremental ingestion path when safe."
    - "Unsupported or ambiguous diffs fall back automatically to the existing full rebuild path."
    - "Graph build, simulation, and reporting flows remain functional after incremental-update support lands."
    - "Incremental-update behavior is documented well enough for future phase planning and verification."
  artifacts:
    - path: backend/app/models/project.py
      provides: "Chunk signature storage for incremental diffing"
      contains:
        - "chunk_signatures"
    - path: backend/app/services/text_processor.py
      provides: "Chunk diffing logic"
      contains:
        - "diff_chunks"
    - path: backend/app/services/graph_builder.py
      provides: "Incremental ingestion worker"
      contains:
        - "incremental_add_text_batches"
    - path: backend/app/api/graph.py
      provides: "Build endpoint with incremental routing and fallback"
      contains:
        - "incremental flag handling"
        - "fallback logic"
  key_links:
    - from: "backend/app/api/graph.py"
      to: "backend/app/services/text_processor.py"
      via: "chunk diffing before build decision"
      pattern: "diff_chunks"
    - from: "backend/app/services/graph_builder.py"
      to: "backend/app/models/project.py"
      via: "chunk signature storage"
      pattern: "chunk_signatures"
---

# Plan: Incremental Graph Updates

## Goal
Support safe delta ingestion for changed content while preserving existing full rebuild fallback.

## Must-Haves
1. Changed source content can be diffed against prior ingestion state.
2. Only changed chunks are sent through the incremental ingestion path when safe.
3. Unsupported or ambiguous diffs fall back automatically to the existing full rebuild path.
4. Graph build, simulation, and reporting flows remain functional after incremental-update support lands.
5. Incremental-update behavior is documented well enough for future phase planning and verification.

## Tasks

<task>
  <id>1</id>
  <name>Add chunk signature storage and diffing logic</name>
  <files>
    - backend/app/models/project.py
    - backend/app/services/text_processor.py
  </files>
  <read_first>
    - backend/app/models/project.py
    - backend/app/services/text_processor.py
  </read_first>
  <action>
    1. Add `chunk_signatures` field to `Project` model (List[str], default empty list).
    2. Update `Project.to_dict()` and `Project.from_dict()` to serialize/deserialize `chunk_signatures`.
    3. Add `diff_chunks(current_chunks: List[str], stored_signatures: List[str]) -> tuple[List[str], List[str]]` to `text_processor.py`:
       - Returns `(changed_chunks, new_signatures)`
       - Computes signature for each current chunk
       - Returns chunks whose signatures are not in stored_signatures
       - Returns all new signatures for storage
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/services/test_graph_incremental.py</automated>
  </verify>
  <acceptance_criteria>
    - `Project` stores `chunk_signatures`.
    - `diff_chunks` correctly identifies changed chunks.
    - `diff_chunks` returns all new signatures.
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <name>Wire incremental ingestion and fallback into build flow</name>
  <files>
    - backend/app/services/graph_builder.py
    - backend/app/api/graph.py
  </files>
  <read_first>
    - backend/app/services/graph_builder.py
    - backend/app/api/graph.py
    - backend/app/models/project.py
  </read_first>
  <action>
    1. In `GraphBuilderService`, add `incremental_add_text_batches` method that reuses `add_text_batches` but accepts a `graph_id` of an existing graph instead of creating a new one.
    2. In `backend/app/api/graph.py` build endpoint:
       - Accept `incremental` flag (default false)
       - If `incremental=true` AND project has a `graph_id` AND diff is safe:
         - Split and deduplicate text
         - Call `diff_chunks` with stored signatures
         - If changed chunks count is 0 → return skip (no changes)
         - If changed chunks count > 0 AND < 50% of total → use incremental path
         - Store new signatures after successful incremental ingestion
       - If `incremental=false` OR diff is ambiguous → use existing full rebuild path
    3. Fallback conditions:
       - No stored signatures
       - Changed chunks > 50% of total
       - `chunk_size` or `chunk_overlap` changed from previous build
       - `force=true`
       - Any exception during diffing
  </action>
  <verify>
    <automated>cd backend && uv run pytest -q tests/services/test_graph_incremental.py</automated>
  </verify>
  <acceptance_criteria>
    - Incremental path sends only changed chunks to existing graph.
    - Fallback to full rebuild works for ambiguous diffs.
    - No changes detected returns skip response.
    - Existing flows (simulation, reporting) remain functional.
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <name>Document incremental behavior</name>
  <files>
    - .planning/phases/07-incremental-graph-updates/07-INCREMENTAL.md
  </files>
  <read_first>
    - backend/app/services/graph_builder.py
    - backend/app/api/graph.py
  </read_first>
  <action>
    1. Write `07-INCREMENTAL.md` documenting:
       - How chunk diffing works
       - When incremental path is used vs fallback
       - API changes (new `incremental` flag)
       - Safety guarantees
  </action>
  <verify>
    <automated>Documentation is complete and accurate.</automated>
  </verify>
  <acceptance_criteria>
    - Documentation explains diffing, fallback, and API changes.
    - Documentation is clear enough for future phase planning.
  </acceptance_criteria>
</task>

## Verification Criteria

- `cd backend && uv run pytest -q tests/services/test_graph_incremental.py` passes.
- Incremental path sends only changed chunks.
- Fallback to full rebuild works.
- Existing simulation and reporting flows remain functional.

## Success Criteria

- Phase 7 ships with incremental graph updates, safe fallback, and clear documentation.
